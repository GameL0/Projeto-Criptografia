#include <stdio.h>

int main()
{
    int entrada;
    scanf("%d", &entrada);
    if (entrada < 1 && entrada > 3)
    {
        printf("Opção inválida\n");
        return 0;
    }

    else
    {
        
    }
}